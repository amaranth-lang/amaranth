[build-system]
requires = ["setuptools>=42", "wheel", "setuptools_scm[toml]>=3.4", "setuptools_scm_node_and_dirty_tag"]

[tool.setuptools_scm]
version_scheme = "guess-next-dev"
local_scheme = "node-and-dirty-tag"
